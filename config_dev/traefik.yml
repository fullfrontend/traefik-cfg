api:
    dashboard: true

log:
    level: INFO

accessLog: { }


entryPoints:
    web:
        address: ":80"


providers:
    docker:
        watch: true
        exposedByDefault: false
        network: "web"
    file:
        directory: "/etc/traefik/dynamic"
