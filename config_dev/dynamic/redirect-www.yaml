http :
    middlewares :
        redirect-www :
            redirectRegex :
                regex : "^https?://www\\.(.+)"
                replacement : "http://${1}"
                permanent : true
